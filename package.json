{
  "name": "pigeon-defenders",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && python3 -m http.server 8001 --directory dist",
    "preview": "python3 -m http.server 8001 --directory dist"
  },
  "dependencies": {}
}
